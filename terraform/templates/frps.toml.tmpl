bindPort      = 7000
vhostHTTPPort = 8080
subdomainHost = "${domain}"

[auth]
  method = "token"
  token  = "${frp_token}"

[webServer]
  addr = "0.0.0.0"
  port = 7500
  user = "admin"
  password = "change-me"
